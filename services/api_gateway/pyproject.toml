[project]
name = "api-gateway"
version = "0.1.0"
description = "API gateway for http requests"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.129.0",
    "faststream[cli,nats]>=0.6.6",
    "uvicorn>=0.40.0",
]
